docs+test: real research pipeline, live agent, and regression coverage

Task 9 - test_suite.py (11 tests) gains:
- test_08_no_hardcoded_research: fails if any of the nine banned hardcoded
  artefacts reappears, and asserts the real research nodes exist.
- test_09_evidence_grounding: every returned claim must cite an evidence_id
  present in `sources`, must carry a quote, and claims_dropped must be 0.
- test_10_demand_signals_carry_urls: no signal may exist without a real url.
- test_11_live_agent_chat: posts to the chat queue and requires a reply that
  made at least one real tool call.
Also migrated the remaining /api/simulate call sites to /api/research and raised
timeouts, which were sized for the old instant path and are far too short now
that a run does real retrieval plus an LLM call.

Task 10 - docs brought back in line with reality:
- CLAUDE.md: documents the N1-N7 research pipeline, why fabrication is
  structurally impossible, the DeepSeek model choice and its reasoning-token
  trap, the live Mastra agent and its five real tools, the updated API surface
  and Helix routes, and the verification commands.
- PLAN.md: the Mastra layer no longer claims fabricated tools; the engine
  nodes, the invariants, and the open items are stated; milestones are ticked
  against what was actually verified.
- Added the Mastra addendum to the plan doc, recording the four real pitfalls
  found (Host header dropped by fetch, MCP outputSchema validation, the
  /responses vs chat-completions default, and ESM hoisting vs dotenv).